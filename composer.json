{
  "name": "content-catalyst-llc/sustainable-catalyst-engagement-intake",
  "description": "Private consulting and contact intake with human administrative review, secure quarantine operations, scanner readiness, and Microsoft Teams preferences.",
  "type": "wordpress-plugin",
  "license": "GPL-2.0-or-later",
  "require": {
    "php": ">=8.1"
  },
  "scripts": {
    "lint": "find sustainable-catalyst-engagement-intake -name '*.php' -print0 | xargs -0 -n1 php -l",
    "test": "php tests/smoke.php && php tests/validator-fixtures.php && php tests/storage-fixtures.php && php tests/upload-environment-fixtures.php && php tests/scanner-fixtures.php && php tests/quarantine-operations.php && php tests/review-schema-fixtures.php && php tests/review-operations.php && php tests/schema-mapping.php"
  }
}
